services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: cyder_music
      POSTGRES_USER: cyder_music
      POSTGRES_PASSWORD: cyder_music_dev
    ports:
      - "5432:5432"
    volumes:
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
      - cyder-music-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\"",
        ]
      interval: 5s
      timeout: 5s
      retries: 20

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: cyder-music:local
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      APP_DATABASE_URL: postgres://cyder_music:cyder_music_dev@postgres:5432/cyder_music
    ports:
      - "8000:8000"
    volumes:
      - cyder-music-data:/data

volumes:
  cyder-music-data:
  cyder-music-postgres-data:
